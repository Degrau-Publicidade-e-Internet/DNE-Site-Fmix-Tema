on:
  push:
    branches:    
      - 'producao'
    paths: 
      - 'Themes/DefaultClean/**'
name: 🚀 FTP Deploy
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v3
    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: 187.0.210.242
        username: FarmaciaMix_FTP
        password: ${{ secrets.FTP_PASSWORD_PROD_FMIX }}
        local-dir: Themes/
        server-dir: httpdocs/Themes/
        timeout: 99999
      
